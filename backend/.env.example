# Port for the API server (default 3001)
PORT=3005

# Path to SQLite database file (default: backend/data/ary.sqlite)
# DATABASE_PATH=./data/ary.sqlite

# Comma-separated list of allowed CORS origins (required when frontend is on another host, e.g. Vercel)
# Example: https://your-app.vercel.app,https://your-app-*.vercel.app
# Leave unset for development (permissive).
# CORS_ORIGINS=https://ary-v0.vercel.app

# Node environment (development | production)
NODE_ENV=development
