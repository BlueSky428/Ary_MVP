{
  "name": "ary-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "Ary V0 — Guided Protocol Pilot (MVP)",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "start:backend": "npm run build --workspace=backend && npm run start --workspace=backend",
    "start:backend:prod": "npm run build --workspace=backend && npm run start:production --workspace=backend",
    "build": "npm run build --workspaces --if-present",
    "build:backend": "npm run build --workspace=backend",
    "build:frontend": "npm run build --workspace=frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
